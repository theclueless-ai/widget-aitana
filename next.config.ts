import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // El widget puede embeberse en otros sitios vía <iframe>.
  // No bloqueamos el framing para permitir el embebido del MVP.
};

export default nextConfig;
